<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0-windows</TargetFramework>
    <UseWPF>true</UseWPF>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <LangVersion>latest</LangVersion>
    <RootNamespace>FTDNG.Modules.Counter.Wpf</RootNamespace>
    <AssemblyName>FTDNG.Modules.Counter.Wpf</AssemblyName>
    <!-- Module Counter: ví dụ tối giản để học luồng module -> Host (không dùng bus). -->
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\..\Shared\FTDNG.SharedKernel\FTDNG.SharedKernel.csproj" />
    <ProjectReference Include="..\..\..\Shared\FTDNG.Contracts\FTDNG.Contracts.csproj" />
  </ItemGroup>

</Project>
